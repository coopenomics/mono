# Нативные prebuild-бинарники из vendor/prebuilds/ — git не должен трогать
# в них переводы строк, иначе файл в репозитории тихо разъезжается с рабочим
# и загрузка падает на ERR_DLOPEN_FAILED.
*.node binary

# Шрифты для генератора документов — по той же причине.
*.ttf binary
*.otf binary
