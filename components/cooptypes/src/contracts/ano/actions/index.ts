/**
 * Выдать или продлить заверение: заверяющий признаёт, что субъект вправе
 * заверять указанным ключом.
 */
export * as Endorse from './endorse'

/**
 * Прекратить признание: убрать запись о заверении, чтобы оно перестало
 * продлеваться и погасло в пределах своего срока.
 */
export * as Revoke from './revoke'

/**
 * Пустышка для обновлений сборки.
 * @private
 */
export * as Migrate from './migrate'

/**
 * Опубликовать новую версию единой схемы весов репутации (только АНО).
 */
export * as Pubrepschema from './pubrepschema'
