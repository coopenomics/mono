import { defineConfig } from 'vitest/config'

/**
 * Тесты парсера импортируют `src/config`, а тот на уровне модуля требует девять
 * переменных окружения и бросает исключение, если хоть одной нет. На машине
 * разработчика их подставляет `.env` через dotenv, поэтому локально тесты
 * проходили, а в CI (где `.env` нет и быть не должно) падали на импорте:
 * «Env variable API is required» — не добравшись ни до одного теста.
 *
 * Значения ниже — заведомо нерабочие заглушки. Сами тесты проверяют только
 * константы `subscribedContracts` / `subsribedActions`, которым окружение не
 * нужно вовсе; env требуется исключительно из-за побочного эффекта импорта.
 * Подставляем его здесь, чтобы прогон не зависел от `.env` разработчика и вёл
 * себя одинаково локально и в CI. Соединений по этим адресам никто не открывает.
 *
 * dotenv не перезаписывает уже заданные переменные, поэтому локальный `.env`
 * эти значения не переопределит — прогон детерминирован.
 */
export default defineConfig({
  test: {
    env: {
      NODE_ENV: 'test',
      API: 'http://127.0.0.1:8888',
      SHIP: 'ws://127.0.0.1:8070',
      MONGO_EXPLORER_URI: 'mongodb://127.0.0.1:27017/cooperative-x',
      START_BLOCK: '1',
      FINISH_BLOCK: '0xFFFFFFFF',
      REDIS_PORT: '6379',
      REDIS_HOST: '127.0.0.1',
      REDIS_PASSWORD: '',
    },
  },
})
