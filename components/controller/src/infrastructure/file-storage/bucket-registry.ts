/**
 * Реестр объявленных хранилищ живёт в `@coopenomics/extension-kit`: объявляет
 * хранилище расширение, создаёт его ядро. Здесь реестр доступен под привычными
 * ядру именами.
 */
export { BucketRegistry, type RegisteredBucket } from '@coopenomics/extension-kit';
