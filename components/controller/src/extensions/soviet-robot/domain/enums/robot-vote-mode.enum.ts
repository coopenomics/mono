/** Режим голосования члена совета по типу решения. */
export enum RobotVoteMode {
  /** Робот голосует «за» сразу при появлении повестки. */
  AUTO = 'auto',
  /** Робот повторяет голос другого члена совета, с тем же знаком и только после него. */
  FOLLOW = 'follow',
}
