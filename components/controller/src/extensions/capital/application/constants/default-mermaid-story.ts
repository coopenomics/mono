/**
 * Минимальный валидный фрагмент Mermaid для пустого тела story при создании (предпросмотр не падает).
 */
// i18n-ignore: стартовая схема Mermaid — содержимое документа, пайщик правит его сам
export const DEFAULT_MERMAID_STORY_SOURCE = `flowchart TD
    A[Старт]`;
