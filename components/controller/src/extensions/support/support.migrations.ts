/**
 * Миграции схемы конфига расширения «Стол поддержки»: явный, упорядоченный
 * список.
 *
 * Порядок здесь — порядок применения. DDL расширения едет вместе с самим
 * расширением, как и его таблицы, а не строками в модуле ядра.
 *
 * Каркас первой фазы: у конфига расширения ещё нет своих полей, версионировать
 * пока нечего — список пуст осознанно.
 */
import type { IExtensionSchemaMigration } from '@coopenomics/extension-kit';

export const supportMigrations: IExtensionSchemaMigration[] = [];
