/**
 * Форма заявки на вступление живёт в `@coopenomics/extension-kit`: её
 * показывает ядро, а расширения от неё наследуются. Здесь она доступна под
 * привычными ядру именами.
 */
export { CandidateOutputDTO, CandidateStatus } from '@coopenomics/extension-kit';
