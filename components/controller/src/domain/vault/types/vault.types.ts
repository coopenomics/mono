/**
 * Разрешения для WIF ключей
 */
export enum wifPermissions {
  'Active' = 'active',
  /** Ключ, которым кооператив заверяет удостоверения пайщиков (отдельно от распорядительного). */
  'Cert' = 'cert',
}
