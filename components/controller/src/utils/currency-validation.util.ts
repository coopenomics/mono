/**
 * Проверка символа валюты живёт в `@coopenomics/extension-kit`: символ берётся
 * из настроек контура, и пользуются проверкой обе стороны. Здесь она доступна
 * под привычным ядру именем.
 */
export { CurrencyValidationUtil } from '@coopenomics/extension-kit';
