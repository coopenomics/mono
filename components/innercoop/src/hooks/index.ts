/**
 * `hooks/` — контракты, которые предоставляет РАСШИРЕНИЕ, а вызывает ядро.
 *
 * Направление обратное `core-ports/`: там ядро отдаёт возможность расширению,
 * здесь расширение отдаёт возможность ядру. Ядро инжектит хуки необязательными
 * — расширения может не быть в кооперативе.
 */
export * from './registration-document-parameters.hook';
