/**
 * `@coopenomics/innercoop` — слой портов ВНУТРЕННЕЙ связи в контуре одного кооператива.
 *
 * Разграничение имён (ADR-01, ADR-02):
 *   - `innercoop` — ядро ↔ расширение и расширение ↔ расширение ВНУТРИ одного кооператива;
 *   - `intercoop` — зарезервировано за федерацией кооператив ↔ кооператив (v4).
 *
 * Пакет не содержит авторизации. Права пайщика проверяются на границе API расширения
 * (guards + CASL по ARCH-2), порт вызывается уже внутри авторизованного use-case.
 */
export * from './core-ports';
export * from './cross-plugin-ports';
export * from './hooks';
