/**
 * `auth/` — guard'ы и декораторы авторизации платформы.
 *
 * Здесь, а не в `@coopenomics/innercoop`, вопреки первичной редакции ADR-06:
 * `innercoop` — пакет контрактов с нулевыми зависимостями, а guard'ы тянут
 * `@nestjs/passport`, `@nestjs/graphql` и `@nestjs/core`. Это каркас, которым
 * расширение защищает собственные резолверы, а не контракт общения с ядром.
 *
 * Права пайщика проверяются здесь, на границе API расширения. Порты `innercoop`
 * вызываются уже внутри авторизованного use-case и своей авторизации не имеют
 * (ADR-16).
 */
export * from './decorators';
export * from './guards';
export * from './server-secret';
