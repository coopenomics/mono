/**
 * Часть полей `T` обязательна, остальные — нет.
 *
 * Применяется к формам частичного обновления: клиент присылает произвольный
 * набор полей, но ключ, по которому запись ищется, обязан быть.
 */
export type RequireFields<T, K> = Partial<T> & Required<Pick<T, Extract<K & keyof T, keyof T>>>;
