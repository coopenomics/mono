/**
 * @brief Миграция данных контракта
 *
 * Сейчас переходов нет, действие ничего не делает. Прежняя миграция заводила
 * расчётный счёт суммой паевого фонда и вступительных взносов, если его
 * остаток был ровно нулём. Она отработала на всех кооперативах один раз
 * (сверено по сети 23.09.2026: у каждого, где счёт заводился, он равен этой
 * сумме), а на каждом следующем деплое оставалась риском — любой кооператив
 * с опустевшим расчётным счётом получил бы начисление второй раз.
 *
 * @ingroup public_actions
 * @ingroup public_ledger_actions

 * @note Авторизация требуется от аккаунта: @p ledger
 */
void ledger::migrate(){
  require_auth(_ledger);
}
