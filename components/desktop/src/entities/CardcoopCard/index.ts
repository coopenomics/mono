/**
 * Карта пайщика в сети «Карта пайщика» (story 7.4).
 *
 * Кооператив знает о карте немного и намеренно: номер, состояние выданного им свидетельства
 * и дату вступления. Ни имени держателя, ни его членств в других кооперативах — это чужие
 * сведения, и сеть их кооперативу не отдаёт.
 */
export { api as cardcoopCardApi } from './api';
export type { ICardcoopCard } from './model';
