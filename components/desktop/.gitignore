.DS_Store
.thumbs.db
node_modules

# Quasar core related directories
.quasar
/dist
/quasar.config.*.temporary.compiled*

# Cordova related directories and files
/src-cordova/node_modules
/src-cordova/platforms
/src-cordova/plugins
/src-cordova/www

# Capacitor related directories and files
/src-capacitor/www
/src-capacitor/node_modules

# BEX related directories and files
/src-bex/www
/src-bex/js/core

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln

# local .env files
.env
# Резервные копии окружения (.env.backup-prod и подобные) — в репозиторий не
# кладём: там боевые адреса и ключи. Ловушка реальная — такой файл лежал в дереве
# неотслеживаемым и однажды чуть не уехал в коммит вместе с `git add`.
.env.*
!.env-example
!.env-testnet
src/shared/config/Env.ts

# Рантайм-конфиг SPA-dev, генерируется docs-harness из .env (см. lib/desktop-config.mjs).
# В проде /config.js отдаёт SSR middleware; статический файл здесь его бы перехватил.
public/config.js
