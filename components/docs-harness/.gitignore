node_modules/
shots/
# Фикстуры с приватными ключами — у каждого разработчика свои,
# генерируются add-plain-participant при первом прогоне.
state/participants/*.json
!state/participants/.gitkeep
state/cooperatives/*.json
!state/cooperatives/.gitkeep

# Расход пула пайщиков для сценария гейта — локальное состояние стенда.
state/gate-used.json
