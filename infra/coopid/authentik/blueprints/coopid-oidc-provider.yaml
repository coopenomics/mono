# CoopID Эпик 5: стандартный OIDC-провайдер кооператива.
#
# Декларативно создаёт OAuth2/OpenID-провайдера + приложение `coopid`, чтобы
# authentik выставил стандартные OIDC-endpoints (discovery, jwks, authorize,
# token, userinfo, revoke, introspect, end-session) для любого RP.
#
# Потребители (gitea/chatcoop/semaphore) НЕ настраиваются здесь — провайдер
# универсален, RP цепляется позже отдельным client'ом (по решению владельца
# 2026-06-13: «делаем только CoopID чтоб OIDC работал стандартно»).
#
# Story 5.1 (стандартные endpoints/RS256), 5.2 (auth_code+PKCE, без implicit/ROPC),
# 5.4 (per-coop issuer через домен).
version: 1
metadata:
  name: CoopID - OIDC provider & application
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  # ── Кастомный scope coop:verification_types ──────────────────────────────
  # coopname + типы верификации пайщика. Источник — атрибуты authentik-юзера,
  # которые проставляет registration-flow (Эпик 1). Defensive .get — провайдер
  # остаётся валиден даже до заполнения атрибутов.
  - identifiers:
      name: CoopID OIDC scope - coop:verification_types
    model: authentik_providers_oauth2.scopemapping
    attrs:
      name: CoopID OIDC scope - coop:verification_types
      scope_name: "coop:verification_types"
      description: Кооператив пайщика и его типы верификации (CoopID)
      expression: |
        attrs = request.user.attributes or {}
        return {
            "coopname": attrs.get("coopname"),
            "verification_types": attrs.get("verification_types", []),
        }

  # ── OAuth2/OpenID-провайдер ──────────────────────────────────────────────
  - identifiers:
      name: coopid
    id: coopid-provider
    model: authentik_providers_oauth2.oauth2provider
    attrs:
      name: coopid
      # ПУБЛИЧНЫЙ клиент, а не confidential (решение владельца 2026-08-09).
      # Основной потребитель этого провайдера — рабочий стол пайщика, то есть
      # приложение в браузере. Секрет клиента ему хранить негде: любой «секрет»
      # в SPA доступен пользователю, поэтому confidential для него неисполним.
      # Защита обеспечивается PKCE (S256) — это и есть штатный способ для
      # браузерных клиентов. Внешние сервисы (gitea и прочие) при подключении
      # получат СВОИ провайдеры и останутся confidential, как и задумано.
      client_type: public
      # Фиксируем идентификатор: иначе authentik генерирует случайный, и его
      # пришлось бы вручную вычитывать и переносить в настройки стола после
      # каждой переустановки стенда.
      client_id: coopid-desktop
      # Адрес возврата рабочего стола. Задаётся окружением, потому что зависит
      # от того, как открыт стенд: по проброшенному порту, по IP или по домену
      # кооператива. Формат — список правил сопоставления (authentik 2026.x).
      redirect_uris:
        - matching_mode: strict
          url: !Env [COOPID_REDIRECT_URI, "http://localhost:8108/auth/callback.html"]
      sub_mode: user_uuid
      issuer_mode: per_provider
      include_claims_in_id_token: true
      access_code_validity: minutes=1
      access_token_validity: minutes=5
      refresh_token_validity: days=30
      # НЕЯВНОЕ согласие, а не явное.
      #
      # Этот провайдер обслуживает собственный рабочий стол кооператива — то есть
      # приложение самого кооператива, а не стороннее. Спрашивать у пайщика
      # «разрешаете ли вы кооперативу доступ к вашим данным в кооперативе» —
      # бессмысленная формальность.
      #
      # Есть и техническая причина: вход по паролю доводится тихим запросом
      # (prompt=none) в скрытом кадре. Экран согласия там показать некому, поэтому
      # с явным согласием тихий вход не может завершиться в принципе — он либо
      # отдаёт consent_required, либо молчит до таймаута.
      #
      # Внешние сервисы (gitea и прочие) получат СВОИ провайдеры, и там согласие
      # уместно и остаётся явным — в том числе экран передачи данных при запросе
      # удостоверения пайщика.
      authorization_flow:
        !Find [
          authentik_flows.flow,
          [slug, default-provider-authorization-implicit-consent],
        ]
      invalidation_flow:
        !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      signing_key:
        !Find [
          authentik_crypto.certificatekeypair,
          [name, "authentik Self-signed Certificate"],
        ]
      property_mappings:
        - !Find [
            authentik_providers_oauth2.scopemapping,
            [scope_name, openid],
          ]
        - !Find [
            authentik_providers_oauth2.scopemapping,
            [scope_name, email],
          ]
        - !Find [
            authentik_providers_oauth2.scopemapping,
            [scope_name, profile],
          ]
        - !Find [
            authentik_providers_oauth2.scopemapping,
            [scope_name, "coop:verification_types"],
          ]

  # ── Приложение (без него нет discovery/JWKS URL) ─────────────────────────
  - identifiers:
      slug: coopid
    model: authentik_core.application
    attrs:
      name: CoopID
      slug: coopid
      provider: !KeyOf coopid-provider
      meta_description: Удостоверение пайщика — единый вход кооператива

  # ── Заголовки встроенных потоков ─────────────────────────────────────────
  # Экраны потоков рисует стол (105-30), а заголовок берёт у потока; у встроенных
  # потоков authentik он английский («Welcome to authentik!»). Имена — свои.
  - identifiers:
      slug: default-authentication-flow
    model: authentik_flows.flow
    attrs:
      title: Вход в кооператив
  - identifiers:
      slug: default-source-authentication
    model: authentik_flows.flow
    attrs:
      title: Вход через внешний сервис
  - identifiers:
      slug: default-source-enrollment
    model: authentik_flows.flow
    attrs:
      title: Регистрация через внешний сервис
  - identifiers:
      slug: default-invalidation-flow
    model: authentik_flows.flow
    attrs:
      title: Выход
